# 项目约定

这个文件会在每次对话开始时自动加载，所以这里写的规矩对 AI 一直有效。

## 这是什么项目

Text-to-SQL 数据分析网站：用户用自然语言提问，agent 生成 SQL、查询数据库、返回表格图表和文字结论。

这是一个**学习项目**，作者的目标是掌握 agent 开发和全栈开发能力，并把它作为求职作品。因此**代码是否由作者本人理解，比代码写得多快更重要。**

## 开工前必读

改任何代码之前，先读 `docs/` 下的四份文档：

- `01-requirements.md` —— 做什么、做到什么程度算完成、什么明确不做
- `02-architecture.md` —— 架构、技术选型、agent 循环设计、安全边界
- `03-api-contract.md` —— 前后端接口约定，**这是唯一契约，改接口必须先改文档**
- `04-roadmap.md` —— 当前进度和每个任务的完成标准

## 最重要的一条规矩

**以下两个文件必须由作者本人手写，AI 不要代写：**

- `backend/agent/loop.py` —— agent 主循环
- `backend/db/guard.py` —— SQL 安全检查

原因：这两处是面试必问的内容（「你的 agent 循环怎么设计的？重试几次？怎么防死循环？」「怎么防止 agent 删库？」）。如果是 AI 写的，作者答不上来，整个项目的价值就没了。

AI 在这两个文件上可以做的事：**审查、指出漏洞、解释某个写法的含义、提出改进建议**。不能做的事：直接给出完整实现。

如果作者明确要求「这次你直接写」，可以写，但要在回复里提醒他这块需要自己重写一遍。

## 其余部分可以放心让 AI 写

样板代码、类型定义、CSS 样式、ECharts 配置、测试用例、Docker 配置、README —— 这些交给 AI 效率更高，也不影响面试表现。

## 编码规范

**Python（后端）**

- Python 3.12，类型注解必写
- 所有接口的输入输出用 Pydantic 模型定义，不要裸 dict
- **文件读写必须显式写编码**：`open(path, encoding="utf-8")`。这台机器的系统默认编码是 GBK，不写会出乱码
- 路径用 `pathlib.Path`，不要拼字符串，也不要写死绝对路径
- 密钥只从环境变量读，不许出现在代码里

**TypeScript（前端）**

- 严格模式，不用 `any`
- 组件放 `src/components/`，一个组件一个文件
- 接口返回的数据结构要定义 type，和 `03-api-contract.md` 保持一致

**通用**

- 一次提交只做一件事，提交信息用中文写清楚做了什么
- 注释写「为什么这么做」，不写「这行在干什么」

## 常用命令

```bash
# 后端（在 backend/ 目录下）
uvicorn main:app --reload --port 8000

# 前端（在 frontend/ 目录下）
pnpm dev

# 生成示例数据库（在项目根目录）
python scripts/seed_db.py

# 跑评测（第 4 周才有）
python scripts/eval.py
```

## 这台机器的环境注意事项

- 系统 ANSI 代码页是 GBK（936），编码问题高发，读写文件一律显式指定 UTF-8
- Python 解释器在 `D:\anaconda\anaconda3.12\python.exe`
- 项目故意放在 `D:\code\` 而不是用户目录下，因为用户名含中文会引发路径问题

## 和作者协作的方式

- 一次改动控制在一个文件、一个功能，方便作者 review
- 改完要用一两句话说明改了什么、为什么，不要只说「已完成」
- 如果作者的要求和 `docs/` 里的文档冲突，先提出来问，不要默默按新要求做
- 作者是新手，遇到他可能不懂的概念主动解释一句，但不要长篇大论
