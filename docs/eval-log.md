# 评测日志

每轮评测一条记录：日期、模型、commit、指标、错题归因。第 4 周的每一轮优化都以此为对照。
记录格式遵循 docs/06-evaluation.md 第八节。

---

## 基线 #1 · eval_20260914105118（record）/ eval_20260914112737（replay 校准）

- **日期**：2026-09-14
- **模型**：ark-code-latest（火山方舟，responses 协议）
- **代码**：commit `51d8d47` + 题集/比对器修订（见下）
- **模式**：record（真调模型并录制 cassette）；同批 cassette replay 重放校准
- **题集**：30 题 · 六层各 5 题（gold.jsonl v3）

### 两个数字都要看

| 口径 | 总准确率 | 自信错答率 | 说明 |
|---|---|---|---|
| 基线 #1（原口径：列数必须一致 + gold 有 ORDER BY 即校验行序） | 76.7%（23/30） | 16.7% | eval_20260914105118 |
| 基线 #1b（校准口径：列投影 + 行序仅限排行榜） | **93.3%（28/30）** | **6.7%** | eval_20260914112737 |

校准依据：7 道错题逐一查 trace（agent 实际 SQL 与结果）后判定，6 道为「数字全对但交付风格不符」（多给说明列 / 未按 gold 顺序排序）——评分器把交付风格问题当成了答案错误。评分口径已写入 docs/06-evaluation.md 第三节并冻结，之后优化轮全部用校准口径。

### 分层（校准口径）

| 层级 | 通过 |
|---|---|
| 简单聚合 | 5/5 |
| 时间筛选 | 5/5 |
| 多表 JOIN | 4/5 |
| 口径陷阱 | 5/5 |
| 不可答 | 4/5 |
| 分组 TopN | 5/5 |

### 指标（校准口径）

- 拒答率（仅可答题过度拒答）：0.0%（硬上限 12%）
- 平均尝试 1.10 次 | 平均耗时 457 ms（replay）/ 10,676 ms（record）
- tokens：in 43,603 / out 21,798

### 7 道错题的逐题判决（3F 归因，基于 trace-dump 证据）

| 题 | agent 实际表现 | 归类 |
|---|---|---|
| C3 | 客单价计算全对（COUNT DISTINCT 用对），多给 2 列中文别名过程列，无法按名投影 | 聚合粒度对；**输出纪律错**（列名别名 + 多给列）→ 留给优化轮 |
| C4 | 数字全对、COALESCE 正确，没排序 | 冤案（已由校准口径修正） |
| D2 | 毛利公式 (unit_price-cost)×quantity 全对，没排序 | 冤案（已修正） |
| D4 | 未知组 94 人正确出现，没排序 | 冤案（已修正） |
| E4 | 直接算出利润率 36.41% 交付 | **真错：该澄清不澄清**（口径歧义题）→ 优化轮主靶 |
| F1 | GROUP BY c.id, c.name 比 gold 原来的 GROUP BY c.name 更正确（库里 26 对重名客户） | **gold 缺陷**：v2 gold 把重名客户合并了，v3 修正 gold |
| F3 | 前 3 贵全对，多给 id/category 列 | 冤案（已修正） |

### 首轮观察

1. **「错得安静」零发生**：D2 用了成交价、C3/D3 去重了订单、D4 未知组出现 —— 三个招牌陷阱全部躲过，guard + lint + prompt 的口径约束在真实模型上生效。
2. **真失败模式只有两种**：① 输出纪律（C3 的中文别名列）；② 拒答校准（E4 该澄清不澄清）。第 4 周优化直接对这两个打。
3. **自愈循环几乎未被触发**（平均 1.10 次尝试）—— 优化重点在「一次写对」，不在「重试更聪明」。
4. **评测体系本身也被基线检验了**：比对器列投影缺陷、gold F1 重名缺陷都在首轮暴露 —— 这正是「先跑基线再优化」的价值。

### 修订记录

- 评分口径：列投影 + 行序仅限排行榜（docs/06-evaluation.md 第三节），Vitest 20 用例覆盖
- gold F1：`GROUP BY c.name` → `GROUP BY c.id, c.name`（v2 gold 缺陷，重名客户合并）

---

## 第 1 轮优化 · 输出契约 · eval_20260914120834

- **日期**：2026-09-14
- **模型**：ark-code-latest | **代码**：commit `15b52a4` + 本轮提示词改动
- **模式**：record（提示词变化 → 键全变 → 30 题全部真调并录制新 cassette）
- **改动（只此一处）**：生成提示词新增【输出契约】三条 —— ①只输出必需列，不带过程列；②列别名英文小写下划线，不用中文；③分组结果按业务数值降序，不按分组键排序
- **对照基线**：基线 #1b（eval_20260914112737）

### before / after

| 指标 | before（#1b） | after | Δ |
|---|---|---|---|
| 总准确率 | 93.3%（28/30） | **96.7%（29/30）** | +1 题 |
| 自信错答率 | 6.7% | **3.3%** | -3.4pp |
| 多表 JOIN | 4/5 | **5/5** | +1 |
| 其余五层 | 5/5×4 + 4/5 | 不变 | 0 |
| 平均尝试 | 1.10 | 1.07 | -0.03 |
| tokens | in 43,603 / out 21,798 | in 45,118 / out 25,265 | out +16%（提示词变长 + 输出更规范，可接受） |

**由对转错：无。** 由错转对：C3 —— agent 不再输出中文别名过程列（`会员等级`/`客单价`），评分器按列名可对齐，客单价数字本身在基线 #1 的 trace 里就已验证是对的。

### 结论

1. 三句话提示词把「输出纪律」这类交付风格问题全部治好，且未伤害任何已通过的题 —— 输出 token +16% 是唯一代价。
2. 六层中只剩 E 层失分（E4 口径歧义该澄清不澄清）—— 澄清机制按 roadmap 属第 5 周产品面，第 4 周其余轮次不再有已知的可修靶子。
3. 第 4 周剩余轮次（枚举注入 A/B、规则文本 A/B、自检提示、few-shot）的预期不再是「提分」，而是**量化已有机制的价值边界** —— 由对转错清单在这几轮里尤其重要（关掉规则文本很可能就是一次全量回归实验）。

---

## 第 2 轮 · A/B：枚举注入的价值 · eval_20260914123515

- **日期**：2026-09-14 | **模型**：ark-code-latest | **代码**：`d25e247` + 新增 `ENUM_INJECTION` 开关
- **实验设计**：反事实 A/B —— 其他一切冻结，仅 `ENUM_INJECTION=off`（schema 卡片不再注入低基数列枚举值），30 题全量真调重跑
- **对照组**：第 1 轮输出契约（eval_20260914120834，96.7%）

### 结果：枚举注入身价 = +6.7pp

| 指标 | 开（对照组） | 关（本轮） | Δ |
|---|---|---|---|
| 总准确率 | 96.7%（29/30） | 90.0%（27/30） | **-2 题** |
| 拒答率 | 0.0% | 4.0% | +4.0pp |
| 自信错答率 | 3.3% | 6.7% | +3.4pp |
| 时间筛选 | 5/5 | 4/5 | -1 |
| 分组 TopN | 5/5 | 4/5 | -1 |
| **口径陷阱（D 层）** | 5/5 | **5/5** | **0** |
| input tokens | 45,118 | 42,145 | -6.6% |

**由对转错：B4、F1。** 「与上轮对比」机制上线后抓到的第一批真回归。

### 归因（和预期不同，信息量更大）

1. **预期会崩的 D 层一题没崩**：忘过滤状态 / 标价 vs 成交价那三个招牌陷阱，防住它们的**不是枚举值注入，而是口径规则文本（rulesPromptText）+ 输出契约**。枚举注入的真实防区是另一类错误：值格式幻觉与「不确定就拒答」。
2. **B4 的错法出乎意料**：不是写错 SQL，而是模型没枚举参考后对数据边界失去信心，直接 unanswerable —— 上下文工程的缺失会**推高过度拒答**，而不只是产出错 SQL。这个现象值得写进 README。（4D 里同型现象放大：11 道回归中 8 道以「拒答」而非「错答」形态出现。）
3. **F1** 只返回了 1 列（丢了金额列），值格式类不稳定。
4. 代价量化：开注入多花 6.6% input tokens，换 6.7pp 准确率 + 4pp 过度拒答的收敛 —— **划算，保留**。

### 本实验确立的方法论

每项上下文工程组件都必须经得起「关掉重跑」的质疑；砍不掉的那部分才是它真正的价值。下一轮（4D）用同样方法量化口径规则文本 —— D 层将是它的考题。

---

## 第 3 轮 · A/B：口径规则文本的价值 · eval_20260915012507

- **日期**：2026-09-15 | **模型**：ark-code-latest | **代码**：`959a37c` + 新增 `RULES_INJECTION` 开关
- **实验设计**：对照组=第 1 轮（96.7%）；仅把 R1-R8 规则文本从生成提示词抽出（`rulesPromptText()` 返回空串），lint 代码层引擎保留，30 题全量真调
- **开关实现**：放在 rules.ts 而非 loop.ts —— 受保护文件不动，且所有消费方行为一致

### 结果：规则文本身价 = +36.7pp

| 指标 | 开（对照组） | 关（本轮） | Δ |
|---|---|---|---|
| 总准确率 | 96.7%（29/30） | **60.0%（18/30）** | **-11 题** |
| 拒答率（过度拒答） | 0.0% | **32.0%（超 12% 上限 2.7 倍）** | +32pp |
| 自信错答率 | 3.3% | 3.3% | 0 |
| 时间筛选 | 5/5 | 1/5 | -4 |
| 多表 JOIN | 5/5 | 3/5 | -2 |
| **口径陷阱 D 层** | 5/5 | **2/5** | **-3** |
| 分组 TopN | 5/5 | 3/5 | -2 |
| input tokens | 45,118 | 30,075 | -33%（省 15k，太贵了，绝不砍） |

### trace 证据（replay 重放抓取，零成本）

1. **招牌陷阱复现被代码层拦下**：D2 无规则文本时模型一次写出
   `SELECT category, SUM(price - cost) FROM products` —— 标价减成本、完全没碰订单表，
   正是 R2/R5 设计的靶子原型。lint 引擎当场 block（trace 存了两条块级违规），
   错数字没有到达用户。**提示词教不会的，代码兜住了。**
2. **军规是胆量来源**：8 题错成「unanswerable 拒答」而非错答（B1 这种直白题也拒）——
   对比 4C（只藏枚举）拒答才 4%，规则文本给模型的确定性值 28pp 的拒答收敛。
3. **自信错答率两轮 A/B 都钉在 3.3%**：36.7pp 的崩盘几乎全部以「诚实拒答」形态出现 ——
   「宁可不敢答、不可安静答错」的设计目标被消融实验正面验证。

### 三轮合成的完整机制图

| 机制 | 身价 | 防什么 | 证据 |
|---|---|---|---|
| 枚举值注入 | +6.7pp | 值格式幻觉 + 部分过度拒答 | 4C |
| 规则文本注入 | +36.7pp | 口径错答 + 大面积不敢答 | 4D |
| lint 代码引擎 | —（不出错时看不见） | 提示词失效时的最后防线 | 4D trace D2 |
| 输出契约提示 | +1 题 | 交付风格 | 第 1 轮 |

结论：上下文工程不是玄学，每一项都有标价；两套「防错答 vs 防不敢答」机制正交互补，缺一不可。

---

## 第 4 轮 · A/B：生成后自检审计 · eval_20260915023943

- **日期**：2026-09-15 | **代码**：`8b8d422` + loop.ts 新增步骤 6.5（审计员）+ `SELF_CHECK` 开关（默认 **off**）
- **实现**：SQL 通过 guard/lint/EQP 后、执行前，追加一次「审计员」LLM 调用（只报疑不改写，疑点走既有修复通道；每 run 最多一次）
- **实验设计**：对照组=第 1 轮 96.7%；仅开 SELF_CHECK。省钱点：生成调用命中既有 cassette，真调的只有审计调用

### 结果：零增益、双倍输出成本

| 指标 | 对照组 | 开自检 | Δ |
|---|---|---|---|
| 总准确率 | 96.7% | 96.7% | **0** |
| 由对转错 / 由错转对 | — | 无 / 无 | 0 |
| 平均尝试 | 1.07 | 1.07 | 0 |
| input tokens | 45,118 | 57,147 | **+27%** |
| output tokens | 25,265 | 51,365 | **+103%** |

### 归因

1. 审计员对 29 道正确题全部 pass，对 E4 也没报 revise —— 符合架构预期：**审计的考题是「这条 SQL 写得对不对」，而 E4 的错误是「这题根本不该答」**，问题不在同一层。自检救不了拒答校准，正如规则文本救不了输出纪律。
2. 成本侧没有惊喜：多一次调用就是多一次调用。默认保持 off。
3. **这一轮的「无用」本身就是结论**：五轮实验里出现了一个负结果轮次，比轮轮「提升」更可信 —— README 会专门写这条。机制分工图补全：「该不该答」属于澄清机制（第 5 周）的职责，不是自检的职责。

### 累计优化曲线

76.7%（原口径）→ 93.3%（评分校准）→ **96.7%（输出契约）** → A/B 定 price tag：枚举 +6.7pp、规则文本 +36.7pp、自检 0（已关）。

---

## 第 5 轮 · A/B：few-shot 开/关 · eval_20260915025850

- **日期**：2026-09-15 | **代码**：`ac92879` + fewshot.ts 检索 + `FEW_SHOT` 开关（默认 off）+ corrections 种子
- **实现**（docs/05 第四节）：样本源 corrections 表（仅 verified_by_user=1 ∧ enabled=1）；打分 = 表重叠 2 分/表 + 中文二元组重叠 1 分/词；阈值 ≥3，**低于阈值宁可不给**；上限 2 条，平分按 id 字典序（确定性）。3 条种子样本刻意避开评测题（防「抄答案」污染 A/B）
- **诚实性检查**：OFF 路径先验证过 —— 种样本后全量 replay 仍 29/30 逐字节命中既有 cassette（few-shot 段落条件展开，空样本不进 prompt）

### 结果：零增益，且让模型变犹豫

| 指标 | 对照组（96.7%） | 开 few-shot | Δ |
|---|---|---|---|
| 总准确率 | 96.7% | 96.7% | 0 |
| 平均尝试 | 1.07 | **1.13** | +0.06（多了自我推翻） |
| input tokens | 45,118 | 51,009 | +13% |
| output tokens | 25,265 | 23,220 | -8% |
| 由对转错 | — | 无 | 阈值门控没让样本带偏任何题 |

### 归因

1. 规则文本（+36.7pp）已经把口径讲得足够清楚，**模型不缺「照抄的样板」，缺的是「规矩」** —— few-shot 的收益空间被规则文本吃掉了。这不是 few-shot 不行，是在强规则上下文里它边际价值趋零。
2. 尝试数 +0.06：示例反而诱发「我觉得可以换个写法」的自我推翻 —— few-shot 的隐性成本，光看准确率看不见。
3. 零回归说明「宁缺毋滥 + verified 门槛」的检索设计是对的：**坏样本没进来，只是好样本也没用**。默认保持 off。

## 第 4 周总结（五轮全录）

| 轮 | 机制 | 身价 | 去向 |
|---|---|---|---|
| 1 | 输出契约提示 | +1 题（93.3→96.7%） | 保留 |
| 2（4C） | 枚举值注入 | +6.7pp | 保留 |
| 3（4D） | 规则文本注入 | +36.7pp | 保留 |
| 4（4E） | 生成后自检 | 0，output +103% | 关 |
| 5（4F） | few-shot | 0，尝试 +0.06 | 关 |

保留的 3 项、否决的 2 项，全部有全量 A/B 数据背书。剩余错题仅 E4（「该不该答」层，第 5 周澄清机制的职责）。

---

## 第 5 周 · 5A 澄清机制 · eval_20260915082445

- **日期**：2026-09-15 | **代码**：`243375f` + clarify.ts 词典 + loop 步骤 2.5
- **机制**：口径歧义确定性词典（利润率/复购率，各带 disambiguators 放行词与 2-3 个候选口径）。命中即在**调模型之前**拒答，澄清选项经 state 事件的 `clarifications` 字段（第 2 周已预留的契约）下发 —— 零 token、零延迟、同题永远同判决
- **对照**：4F 轮（96.7%，E4 唯一错题）

### 结果：30/30 = 100.0%

| 指标 | 对照 | 本轮 | Δ |
|---|---|---|---|
| 总准确率 | 96.7%（29/30） | **100.0%（30/30）** | +1 题 |
| 自信错答率 | 3.3% | **0.0%** | -3.3pp |
| 不可答层 | 4/5 | **5/5** | +1 |
| 其余五层 | 5/5 | 5/5 | 0 |
| 由对转错 | — | **无** | 词典零误伤（含「毛利率」不误伤的边界测试） |

### 诚实性说明（必须记录）

1. **这 100% 不是"把模型调到全对"**：28 道 answered 题命中第 1 轮 cassette 原样通过；E3/E4 由词典拒答。唯一的变化是把"口径歧义题的正确行为"从碰运气变成了**代码保证**——这正是产品主张「会对错口径说不」在该类问题上的落实。
2. **词典只有 2 个词条**（利润率/复购率），覆盖的是"指标名即歧义"这一类问题的确定性解法——真实 BI 产品同样维护指标口径字典。词条是数据，扩充零成本；但它不解决"模型理解错表结构"等其他类问题，那些已有 lint/guard/三态分守。
3. **对比基线说明**：本轮「由错转对 A1」是对照了快照防篡改测试那条脏 run（eval_20260915034104，A1 因人为篡改快照而失败），并非真实修复。
4. 拒答题的澄清选项前端展示属 5B。

### 累计曲线（评测口径冻结后）

93.3%（校准基线）→ 96.7%（输出契约）→ **100.0%（澄清机制）**；两轮 A/B 否决项（自检/few-shot）保持关闭。

---
