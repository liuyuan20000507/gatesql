# ADR-012 · 不用任何 agent 框架

**背景**：LangChain / LangGraph / LlamaIndex 能显著减少代码量。

**决策**：手写主循环，零框架。

**关键问题**：项目的核心价值就是「我设计了这个循环并能讲透它」——一行 import 换掉全部价值。且 CLAUDE.md 明确主循环作者手写。

**代价**：多写约 250 行，自己处理结构化输出容错、重试分流、预算管理。**这些恰恰是面试要问的部分——代价即收益。**
