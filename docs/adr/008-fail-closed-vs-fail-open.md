# ADR-008 · 安全 fail-closed，口径 lint fail-open

**背景**：guard 和 lint 用同一个解析器，都会遇到解析失败。

**决策**：**同一个解析器，两条相反的失败策略。** guard 解析失败即拒绝；lint 解析失败放过并记 warn。

**关键问题**：失败方向的收益相反——安全上「拒绝」是安全侧（解析不了的 SQL 宁可不执行）；可用性上「放过」是安全侧（口径检查失败不该杀掉一个可能完全正确的查询）。

**代价**：这个刻意的不对称必须在代码里写注释，否则后来者会当 bug 修掉。
